/* eslint-disable react-refresh/only-export-components */
import ReactDOM from "react-dom/client";
// eslint-disable-next-line react-refresh/only-export-components
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(<App />);

//klamry wykorzystujemy do przekazywania dynamicznych wartosci do tagow, a takze wartosci atrybutow
//Komponenty to funckje ktore poprzez stworzenie instancji ktora zwraca element.
//Czyli komponent -> Funckja, Instancja komponentu -> wywolanie funcji, element-> wartosc zwrotna przy wywolaniu funkcji.
